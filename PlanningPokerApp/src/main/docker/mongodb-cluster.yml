version: '2'
services:
  planningpokerapplication-mongodb:
    image: mongo:4.0.13
    ports:
      - '27017:27017'
    command: mongos --configdb planningpokerapplication-mongodb-config
  planningpokerapplication-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --replSet rs1 --noprealloc --smallfiles
  planningpokerapplication-mongodb-config:
    image: mongo:4.0.13
    container_name: planningpokerapplication-mongodb-config
    command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
